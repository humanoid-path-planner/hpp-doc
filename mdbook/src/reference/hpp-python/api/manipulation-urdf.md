# `pyhpp.manipulation.urdf`

## Functions

| def | Description |
|:---|:---|
| <code><span class="hljs-keyword">def</span> <span class="hljs-title function_">loadModel</span>(arg1: <a href="manipulation.md#device">pyhpp.manipulation.bindings.Device</a>, arg2: <span class="hljs-built_in">int</span>, arg3: <span class="hljs-built_in">str</span>, arg4: <span class="hljs-built_in">str</span>, arg5: <span class="hljs-built_in">str</span>, arg6: <span class="hljs-built_in">str</span>, arg7: pinocchio.pinocchio_pywrap_default.SE3) -&gt; <span class="hljs-literal">None</span></code> | Load a robot model from URDF/SRDF files into a manipulation Device, also parsing grasp/contact data from the SRDF. |
| <code><span class="hljs-keyword">def</span> <span class="hljs-title function_">loadModelFromString</span>(arg1: <a href="manipulation.md#device">pyhpp.manipulation.bindings.Device</a>, arg2: <span class="hljs-built_in">int</span>, arg3: <span class="hljs-built_in">str</span>, arg4: <span class="hljs-built_in">str</span>, arg5: <span class="hljs-built_in">str</span>, arg6: <span class="hljs-built_in">str</span>, arg7: pinocchio.pinocchio_pywrap_default.SE3) -&gt; <span class="hljs-literal">None</span></code> | Load a robot model from URDF/SRDF XML strings into a manipulation Device, also parsing grasp/contact data from the SRDF. |
