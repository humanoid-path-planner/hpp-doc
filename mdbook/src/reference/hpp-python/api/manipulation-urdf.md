# `pyhpp.manipulation.urdf`

## Functions

| Function | Description |
|:---|:---|
| `loadModel()` | Load a robot model from URDF/SRDF files into a manipulation Device, also parsing grasp/contact data from the SRDF. |
| `loadModelFromString()` | Load a robot model from URDF/SRDF XML strings into a manipulation Device, also parsing grasp/contact data from the SRDF. |
